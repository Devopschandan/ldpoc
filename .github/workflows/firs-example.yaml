name: My first Workflow

on: push

jobs:
    First_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo " My first Github exper"

            - name: Checkout repo
              uses: actions/checkout@v5

            - name: list files
              run: ls

            - name: Read file
              run: cat README.md

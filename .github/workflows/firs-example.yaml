name: My first Workflow

on: push

jobs:
    First_job:
        runs-on: ubuntu-latest
        steps:

            - name: Checkout repo
              uses: actions/checkout@v5

            - name: Welcome message list and read
              run: |
                echo " My first Github exper"
                ls
                cat README.md
            - name: Generate ASCII Artwaork
              run: cowsay -f dragon " Run for cover, I am a dargaon ... " >> dragon.txt
